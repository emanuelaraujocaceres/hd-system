# ══════════════════════════════════════════════════════════════════════
# HD-System Supabase Configuration
# ══════════════════════════════════════════════════════════════════════
#
# Usage:
#   Development:  supabase start (local)
#   Staging:      supabase db push --linked (remote staging project)
#   Production:   supabase db push --linked (remote production project)
#
# To link a project:
#   supabase link --project-ref <project-id>
#
# ══════════════════════════════════════════════════════════════════════

# Project refs (refer�ncia � n�o � se��o v�lida do config.toml da CLI 2.x):
#   production = tixwhmgzibvazkqbqoev
#   staging    = YOUR_STAGING_PROJECT_ID

[db]
port = 54322
shadow_port = 54320
major_version = 15

[studio]
port = 54323

[local_smtp]
enabled = true
port = 54324

[auth]
site_url = "http://localhost:3000"
additional_redirect_urls = ["https://localhost:3000"]
jwt_expiry = 3600
enable_refresh_token_rotation = true
refresh_token_reuse_interval = 10

[analytics]
enabled = false

[experimental]
# pg_ai not enabled
